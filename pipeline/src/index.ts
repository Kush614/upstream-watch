/**
 * Scraper runner entry point.
 *
 * TODO(H2, docs/PLAN.md §4): wire scrape -> cache -> parse -> validate -> diff -> relevance.
 * Stages live in src/lib/, external calls in src/clients/ (CLAUDE.md §7).
 */

export {};

console.log("upstream-watch pipeline — not implemented yet. See docs/PLAN.md §4 H2.");
