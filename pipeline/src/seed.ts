/**
 * `pnpm demo:seed` — loads fixtures and the seeded breaking change (CLAUDE.md §5).
 *
 * TODO(H1, docs/PLAN.md §4): reset demo-app to unpatched, clear last-seen state, and
 * select the seeded-breaking fixture so a cold demo run is reproducible.
 */

export {};

console.log("demo:seed — not implemented yet. See docs/PLAN.md §4 H1.");
