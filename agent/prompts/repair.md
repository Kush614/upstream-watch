The extraction spec for {{vendor}} no longer yields valid entries. You are given: the current raw HTML, the last HTML that worked, the current spec, the JSON schema, and three example entries that were previously extracted. Propose a new extraction spec (same YAML shape) that extracts entries matching the schema from the current HTML, and would still extract the example entries from the last-good HTML where possible. Output the YAML block only.
