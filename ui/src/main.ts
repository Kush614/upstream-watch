/**
 * UI entry point.
 *
 * TODO(H6, docs/PLAN.md §4): mount @truefoundry/trueforge-ui plus the two panels in
 * src/panels/ (specs/ui.md). Intentionally not importing React yet — the embedding
 * story for trueforge-ui is unverified (docs/PLAN.md §8, item 4), and cut #4 may
 * retire this package entirely in favour of the stock approval UI.
 */

export {};
