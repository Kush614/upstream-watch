/**
 * The victim.
 *
 * TODO(H1, docs/PLAN.md §4): one file, one function, one test — a call to the watched
 * vendor API using the exact parameter the seeded changelog entry deprecates. Keep it
 * tiny: the diff has to be readable on camera.
 */

export {};
