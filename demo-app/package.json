{
  "name": "demo-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The watched code. Small enough to read on a projector, real enough that patching it counts.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
