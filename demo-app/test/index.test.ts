import { describe, it, expect } from "vitest";

/** TODO(H1): assert the vendor call is shaped correctly, so the patch has something to prove. */
describe("demo-app", () => {
  it("has a working test harness", () => {
    expect(true).toBe(true);
  });
});
